export interface ReportService {
  initialize: () => void
}

export const reportService: ReportService = {
  initialize: () => {
    // Placeholder: business logic will be added in future iterations.
  }
}